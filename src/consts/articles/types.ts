export type ArticleListSchema = {
  code: string;
  menu_display_name: string;
  language: string;
};

export type ArticleDetailSchema = {
  code: string;
  menu_display_name: string;
  language: string;
  html_content: string;
  title: string;
};
